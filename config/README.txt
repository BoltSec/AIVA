Drop your downloaded CISA file here as:
    known_exploited_vulnerabilities.json
Then set AIVA_KEV_FILE=config/known_exploited_vulnerabilities.json
(see .env.example). AIVA will use this local copy instead of fetching CISA.
